#include <stdio.h>
/**
 * main - entry point
 * description: 'print single digit numbers of base ten'
 * Return: always 0
 */
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		printf("%d", i);
	}
	printf("\n");


	return (0);
}
